
var config = {
  // Folder where swagger schema is stored
  "swaggerFolder": "data",
  // Folder where documentation is generated
  "docFolder": "docs",
  // List of how much of the REST API schema is done
  "coverageFile": "coverage.md"
};

module.exports = config;
